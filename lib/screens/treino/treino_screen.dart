import 'package:flutter/material.dart';

class TreinoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tela de Treino'),
    );
  }
}
