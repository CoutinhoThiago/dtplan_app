import 'package:flutter/material.dart';

class DietaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Tela de Dieta'),
    );
  }
}
